version: "2"

sql:
  - engine: postgresql
    schema: db/migrations
    queries: db/queries
    gen:
      go:
        package: postgresdb
        out: internal/platform/postgres/generated
        sql_package: pgx/v5
        emit_interface: true
        emit_empty_slices: true
        omit_unused_structs: true
